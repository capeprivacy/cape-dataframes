label: spark-round-float
version: 1
transformations:
      - name: roundFloat
        type: numeric-rounding
        dtype: Float
        precision: 0
rules:
  - match:
      name: ones
    actions:
      - transform:
          name: roundFloat
